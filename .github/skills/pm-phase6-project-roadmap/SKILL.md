---
name: pm-phase6-project-roadmap
description: 第6段階でプロジェクトロードマップを作成する。マイルストーン、依存関係、検証順序、リスクを時系列で整理するときに使う。
---

# Project Roadmap

- 出力は Markdown 表を基本にし、必要なら Mermaid `timeline` を併記する。
- 列は `マイルストーン / 目的 / 完了条件 / 依存 / リスク` を使う。
- 実装順ではなく、学習価値と依存関係を優先して並べる。
- MVP 到達条件を 1 つ明示する。
