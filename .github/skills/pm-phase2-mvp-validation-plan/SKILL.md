---
name: pm-phase2-mvp-validation-plan
description: 第2段階でMVP検証プランを作成する。スモークテスト、フェイクドア、説明動画、LP検証などを最小コストで設計するときに使う。
---

# MVP Validation Plan

- 出力は `仮説 / 対象 / 実験方法 / 計測指標 / 成功閾値 / 期間 / 停止条件 / 次の意思決定` の順で作る。
- 実験方法は実装コストが低い順で候補を比較する。
- 指標は `入力指標` と `結果指標` を分ける。
- 最後に「学びが最大で工数が最小の実験」を 1 つ推奨する。
